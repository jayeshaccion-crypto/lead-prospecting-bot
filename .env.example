# Lead Prospecting Bot - Environment Configuration
#
# DB_PATH: Path to the SQLite database file (default: data/leads.db)
# TARGETS_CONFIG: Path to the targets YAML file (default: config/targets.yaml)
#
# Copy this file to .env and customize:
#   cp .env.example .env

DB_PATH=data/leads.db
